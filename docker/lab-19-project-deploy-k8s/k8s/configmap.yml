apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: todo-app
data:
  NODE_ENV: "production"
  DB_HOST: "db-service"
  DB_PORT: "5432"
  DB_NAME: "todos"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
